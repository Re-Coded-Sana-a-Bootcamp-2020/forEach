// Defin an array with min of 3 name in small letters
const names = ['muhanned','maher','salah'];
// This array will contain your names after turning them into upper case.
let uppercaseNames = [];

// With a for loop you might do something like this.
// for (let i = 0; i < names.length; i++) {
//   uppercaseNames.push(names[i].toUpperCase());
// }


// Do the same thing the upper function does using forEach function.

console.log(uppercaseNames)
